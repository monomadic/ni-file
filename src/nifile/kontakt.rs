use crate::kontakt::chunkdata::ChunkData;

pub struct KontaktInstrument(pub Vec<ChunkData>);

impl KontaktInstrument {
    // pub fn zones(&self) -> Result<Vec<Zone>, Error> {
    //     todo!()
    // }
}

pub struct Zone {}
